unit uBiblioteca;

interface
uses IniFiles;
  procedure setValorIni(pLocal, pSessao, pSubSessao : string; pValor  : string);
implementation
  procedure setValorIni(pLocal, pSessao, pSubSessao : string; pValor  : string);
  var vArquivo : TIniFile;
     Varquivo := TIniFile.Create(ExtractFilePath());
  end;
end.
